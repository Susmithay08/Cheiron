[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q -m "not live"
markers =
    live: hits the real ClinicalTrials.gov API (deselected by default with -m "not live")
